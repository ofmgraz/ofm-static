// holds shared config for all tabulator-js tables

var config = {
    //height: 800,
    //minWidth: 700,
    // layout: "fitColumns",
    layout: "fitDataTable",
    tooltips: true,
    dataLoader: true,
    responsiveLayout:"hide",
};
